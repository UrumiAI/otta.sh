---
"@urumi/plugin": patch
---

Rename the plugin commerce console's nav label and in-page headings from
"Products" to "Pricing & inventory" so it reads as a distinct tool from the CMS
"Products" content type. Label-only: the `/products` route, `products:*` action
ids, and all internal identifiers are unchanged.
