# oxfmt ignore file (oxfmt reads .prettierignore).
# Docs are hand-formatted; oxfmt owns code + JSON only.
*.md

# The rendered reference for docs/theme/TEMPERED.md — a FROZEN artifact that
# the spec points at ("open it in a browser and compare against it as you
# build"). It is hand-authored HTML, it is not shipped, and reformatting it
# rewrites ~2600 lines and makes every theme increment's diff unreadable.
docs/theme/tempered-mockup.html

# Regenerated by the EmDash dev server on every start, from the live schema.
# Formatting it means `pnpm format` and `astro dev` fight over the file.
sites/staging/emdash-env.d.ts
